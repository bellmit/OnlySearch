spring:
  application:
    ###application的名称
    name: SpringBoot-Sentinel
  cloud:
    sentinel:
      transport:
        port: 8270 #跟控制台交流的端口,随意指定一个未使用的端口即可
        dashboard: 127.0.0.1:8080 # 指定控制台服务的地址
      eager: true

  profiles:
    active: dev